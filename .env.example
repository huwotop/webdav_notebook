# ============================================================
# Cloudflare Pages 部署说明（GitHub → Pages Git 集成）
# ============================================================
# 本项目【仅支持】推送到 GitHub 后，通过 Cloudflare Pages 的
# Git 集成（Connect to Git）自动构建部署。代码仓库里**有意地
# 删除了 wrangler.toml 文件**，完全采用「控制台全托管模式」：
# 避免 Cloudflare 锁定 Add binding 按钮；同时也不需要任何
# CLI 部署命令（npm run deploy 已移除）。
#
# 部署流程与完整控制台设置步骤请详见 README.md。
#
# 本文件仅做变量名参考。不要在仓库创建 .env 文件！
# 所有变量都在 Cloudflare Pages 控制台设置：
#   Pages 项目 -> Settings -> Environment variables
#
# 【还有一项必做控制台配置：Functions 兼容标志】
#   Pages 项目 -> Settings -> Functions -> Compatibility flags
#   在 Production 环境添加 1 个 flag：
#     nodejs_compat
#   Compatibility date 填 2024-01-01 或更新日期。
#   （原 wrangler.toml 里的配置，现改为控制台设置，否则 WebDAV 请求 500）
#
# ============================================================
# Cloudflare KV 缓存配置（可选，但强烈推荐）
# ============================================================
# Cloudflare KV 可用作 WebDAV 回源前的缓存层，显著降低响应延迟，
# 同时节省 WebDAV 服务器的带宽。免费额度非常充足：
#   100,000 次读取 / 天，1,000 次写入 / 天，1 GB 存储。
#
# 启用步骤（仅一次，全部在控制台操作）：
#   1) 控制台 -> Workers & Pages -> KV -> Create a namespace
#      Name 填: webdav-notepad-cache  (任意名字)
#
#   2) 回到 Pages 项目 -> Settings -> Functions -> KV namespace bindings -> Add binding
#      Variable name:  NOTE_CACHE    （必须完全一致，代码里用这个名字读取）
#      KV namespace:  下拉选中步骤 1 创建的那个 namespace
#
#   3) （可选）在 Environment variables 中添加：
#      KV_CACHE_TTL = 3600    缓存存活秒数，默认 3600 = 1 小时
#
# 完成上述 Settings 设置后，记得去 Deployments 页面 Retry 一次部署
# （或推送一个空 commit），配置才会加载生效。
# 如果未做 KV 绑定，代码会自动跳过缓存直接访问 WebDAV，不报错。
# ============================================================

# WebDAV Configuration
WEBDAV_URL=
WEBDAV_USERNAME=
WEBDAV_PASSWORD=
WEBDAV_PATH=/WebDAV-Notes

# App Customization
SITE_NAME=云端网络记事本
AUTH_PASSWORD=
LOCK_PASSWORD=

# （AI 能力预留位置：Gemini/OpenAI 等接口如需接入请自行二次开发，项目中已移除相关 stub）

# KV Cache TTL in seconds (optional, default 3600 = 1 hour)
KV_CACHE_TTL=3600
